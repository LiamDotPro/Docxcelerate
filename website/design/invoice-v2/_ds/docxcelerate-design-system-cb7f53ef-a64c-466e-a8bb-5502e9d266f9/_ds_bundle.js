/* Stub. The real _ds_bundle.js drives Design System pane chrome on the design
   canvas and contributes nothing to the artboard's own pixels — the invoice
   pages are laid out entirely by their inline styles plus the token CSS. It is
   stubbed here so the fixture renders offline without the canvas runtime.
   Verified: the artboard renders identically with and without it. */
