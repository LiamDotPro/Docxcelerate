---
title: Graph
description: Настоящие диаграммы Word — столбцы, линия, область, круг, кольцо или точки — объявленные как данные, а не нарисованные картинкой.
group: Nodes
subgroup: Node types
order: 13
---

import NodeSummary from "../../../../components/NodeSummary.astro";
import NodeVariants from "../../../../components/NodeVariants.astro";

<NodeSummary type="graph" />

## Объявляется, а не рисуется

Узел графика содержит числа и форму, но никогда — изображение:

```tsx
export const VisitsByMonth: Graph = () => {
  const [state] = useState((data: MemberData) => ({
    months: data.visitsByMonth.map((entry) => entry.month),
    visits: data.visitsByMonth.map((entry) => entry.visits),
  }));

  return (
    <Graph
      id="visits-by-month"
      title="Visits by month"
      graphType="bar"
      valueAxisTitle="Visits"
      data={{
        categories: state.months,
        series: [{ label: "Visits", values: state.visits }],
      }}
      caption="Your visits, last six months"
    />
  );
};
```

В `.docx` попадает **настоящая диаграмма Word** — часть DrawingML со всеми
значениями внутри и книга, которую открывает «Изменить данные». Читатель может
выделить её, изменить оформление и тип, открыть её числа, а Word рисует её
векторами в любом размере. Ничто из этого не растрируется.

## Форма данных

`data` типизирована: категории и ряды, у каждого ряда — подпись, значения и, при
желании, цвет.

```ts
interface GraphData {
  categories?: string[];
  series: Array<{ label?: string; values: (number | null)[]; color?: string }>;
}
```

`null` — это пропуск, а не ноль. Месяц, который ещё не считан, — не месяц, в
котором ничего не намерили: Word пропускает точку, а не роняет линию.

Цвета берутся из темы (`palette.series` — фиксированный набор, проверенный на
цветовую слепоту), поэтому документ можно переоформить, и диаграммы не выпадут
из его оформления.

Одно с данными всё же происходит: строковые значения пропускаются через рендерер
шаблонов, как и проза. Подпись, содержащая `{{derived.total}}`, будет разрешена.

## Вычисления в состоянии

Нарастающие итоги, проценты и пересчёт базы — дело функции `data`, а не того,
что выше по течению. Тогда график и текст рядом с ним считаются из одного
источника и не могут разойтись.

## Варианты

<NodeVariants type="graph" />

## Примечания

- У обоих хелперов `graphType` по умолчанию равен `bar`.
- У динамического графика форма всё равно фиксируется локально. Эндпоинт даёт
  цифры и может переопределить `graphType` и `caption` в своём ответе.
