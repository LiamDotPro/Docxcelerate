---
title: The node model
description: What every node has in common, what makes them differ, and where each one is documented.
group: Nodes
order: 1
---

import NodeTypes from "../../../components/NodeTypes.astro";

A document is a tree of nodes. Every node is three things: an **id**, a **kind**,
and a rule for producing content.

```tsx
<Paragraph          // the kind
  id="greeting"     // the address
>
  Dear {state.name},  {/* the rule */}
</Paragraph>
```

Resolved against your data, it becomes plain JSON:

```json
{ "id": "greeting", "kind": "paragraph", "mode": "static", "text": "Dear Adaeze Nkemelu," }
```

No styling, no layout. Those belong to the renderer.

## The catalog

Each type has a page of its own, with every option it takes and a preview of
each way it can be written.

<NodeTypes />

## Ids are addresses

A node's id is how a generation endpoint targets that paragraph, and how two
build artifacts line up in a diff. Renaming one breaks whatever points at it
from outside, the way renaming an API route does.

Keep them unique across the document. It costs nothing and makes logs readable.

## Where content comes from

Every node either has its content or has prompts for producing it, and that is
decided by the component rather than declared anywhere. Give a paragraph text —
or an `Image` a `src`, or a `Graph` its `data` — and it resolves on your
machine. Give it prompts and a placeholder instead, and it is filled in at
request time.

Both resolve to the same `kind`, differing by `mode` in the built document.
`mode` is output, not input: the build derives it from what the component
supplied, and the package tells an engine which nodes it has to resolve.
Supplying both on one element is an error rather than a coin-toss.

[Writing nodes](/docs/writing-nodes/) covers the prompt slots and what previews
show in their place.

## Nesting

[`section`](/docs/nodes/section/) is the only node that holds children today,
and it accepts any kind, including other sections. A table node with nodes in
its cells is next, on the same principle: containers hold the components you
already write, rather than a second content model beside them.

## About these previews

Every preview here is a real build. `src/nodes/` in this site's repository holds
one file per variant, written against the published package; a build step
resolves each through `buildDocument` and renders it with the renderer
`dxcl dev` serves. The source shown is the file that ran, and the JSON is what
came back — so these pages break loudly rather than going quietly out of date.
